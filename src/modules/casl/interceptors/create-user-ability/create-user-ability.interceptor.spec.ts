import { CreateUserAbilityInterceptor } from './create-user-ability.interceptor';

describe('CreateUserAbilityInterceptor', () => {
  it('should be defined', () => {
    expect(new CreateUserAbilityInterceptor()).toBeDefined();
  });
});
