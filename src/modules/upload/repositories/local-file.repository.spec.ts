import { LocalFileRepository } from './local-file.repository';

describe('LocalFileRepository', () => {
  it('should be defined', () => {
    expect(new LocalFileRepository()).toBeDefined();
  });
});
